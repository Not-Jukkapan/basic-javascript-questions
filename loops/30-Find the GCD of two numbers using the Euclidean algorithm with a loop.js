// Find the GCD of two numbers using the Euclidean algorithm with a loop:

// Input: (48, 18) → Output: 6

// Hint: Use a loop to repeatedly apply the formula GCD(a, b) = GCD(b, a % b) until b becomes zero.