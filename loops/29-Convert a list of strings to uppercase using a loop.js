// Convert a list of strings to uppercase using a loop:

// Input: ["hello", "world"] → Output: ["HELLO", "WORLD"]

// Hint: Use a loop to iterate through the list, converting each string to uppercase using toUpperCase().