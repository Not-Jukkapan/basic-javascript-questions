// Generate the multiplication table for a number using a loop:

// Input: 5 → Output: 5x1=5, 5x2=10, ..., 5x10=50

// Hint: Use a for loop to multiply the number by 1 through 10, printing each result.
