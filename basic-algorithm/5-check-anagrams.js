// Check if Two Strings Are Anagrams:
// Input: "listen", "silent" → Output: True
// Hint: Sort both strings or use a frequency counter to compare character counts.
// Algorithm: Sorting (O(n log n)), frequency counter (O(n)).