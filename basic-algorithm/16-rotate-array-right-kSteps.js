// Rotate Array Right by K Steps:
// Input: [1, 2, 3, 4], k=2 → Output: [3, 4, 1, 2]
// Hint: Reverse the entire array, then reverse the first k elements, and finally reverse the rest.
// Algorithm: Array reversal technique (O(n)).
