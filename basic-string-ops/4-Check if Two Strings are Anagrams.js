// Check if Two Strings are Anagrams:
// Input: "listen", "silent" → Output: True

// Hint: Sort both strings and compare them, or use a hash map to count the frequency of characters in both strings and check if the counts are the same.