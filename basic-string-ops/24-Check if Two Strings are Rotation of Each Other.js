// Check if Two Strings are Rotation of Each Other:
// Input: "abcd", "dabc" → Output: True

// Hint: Concatenate the first string with itself and check if the second string is a substring of the result.