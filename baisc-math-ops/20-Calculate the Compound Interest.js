// Calculate the Compound Interest:
// Input: (principal=1000, rate=5%, time=2) → Output: 1102.5
// Hint: Use the formula A = P(1 + r/n)^(nt).































// JavaScript Example:
// function compoundInterest(principal, rate, time, n = 1) {
//     return principal * Math.pow((1 + rate / n), n * time);
//   }
  
//   console.log(compoundInterest(1000, 0.05, 2));  // 1102.5
  
