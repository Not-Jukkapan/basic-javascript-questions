// Calculate Combinations of n Elements:
// Input: (5, 3) → Output: 10
// Hint: Use the formula n! / (r!(n-r)!).










































// JavaScript Example:
// function combinations(n, r) {
//     return factorial(n) / (factorial(r) * factorial(n - r));
// }

// console.log(combinations(5, 3));  // 10
