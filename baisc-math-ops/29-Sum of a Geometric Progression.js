// Sum of a Geometric Progression:
// Input: (a=1, r=2, n=4) → Output: 15
// Hint: Use the formula Sum = a * (1 - r^n) / (1 - r) for r != 1.







































// JavaScript Example:
// function geometricProgressionSum(a, r, n) {
//     return a * (1 - Math.pow(r, n)) / (1 - r);
// }

// console.log(geometricProgressionSum(1, 2, 4));  // 15
