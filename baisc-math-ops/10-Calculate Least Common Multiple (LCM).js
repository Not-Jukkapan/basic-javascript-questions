// Calculate Least Common Multiple (LCM):
// Input: (4, 5) → Output: 20
// Hint: The formula for LCM is LCM(a, b) = (a * b) / GCD(a, b).





























// JavaScript Example:
// function lcm(a, b) {
//     return (a * b) / gcd(a, b);
//   }
  
//   console.log(lcm(4, 5));  // 20
  