// Multiply Two Numbers Without Using * Operator:
// Input: (3, 4) → Output: 12
// Hint: Use repeated addition or bitwise shifting. The basic idea is to add a to itself b times.

// JavaScript Example:




























// function multiplyWithoutAsterisk(a, b) {
//     let result = 0;
//     while (b > 0) {
//       if (b & 1) result += a;
//       a <<= 1;
//       b >>= 1;
//     }
//     return result;
//   }
  
//   console.log(multiplyWithoutAsterisk(3, 4));  // 12
  