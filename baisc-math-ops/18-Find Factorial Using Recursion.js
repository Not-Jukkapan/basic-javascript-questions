// Find Factorial Using Recursion:
// Input: 5 → Output: 120
// Hint: Recursively multiply the current number by factorial(n - 1) until the base case of n == 0.

























// JavaScript Example:
// function factorialRecursive(n) {
//     if (n === 0) return 1;
//     return n * factorialRecursive(n - 1);
//   }
  
//   console.log(factorialRecursive(5));  // 120
  