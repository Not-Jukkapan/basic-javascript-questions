// Find the square roots of numbers in a list using map:

// Input: [4, 9, 16] → Output: [2, 3, 4]

// Hint: Use .map() with Math.sqrt() to find the square root of each element: arr.map(x => Math.sqrt(x)).