// Check if each string starts with a vowel using map:

// Input: ["apple", "banana"] → Output: [True, False]

// Hint: Use .map() with a regular expression to check if the string starts with a vowel: arr.map(str => /^[aeiouAEIOU]/.test(str)).

