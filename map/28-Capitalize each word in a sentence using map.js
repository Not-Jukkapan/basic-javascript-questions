// Capitalize each word in a sentence using map:

// Input: "hello world" → Output: "Hello World"

// Hint: Split the sentence by spaces, use .map() to capitalize each word, and join them back: sentence.split(' ').map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(' ').
