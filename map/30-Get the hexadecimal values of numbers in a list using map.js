// Get the hexadecimal values of numbers in a list using map:

// Input: [10, 15, 255] → Output: ['a', 'f', 'ff']

// Hint: Use .map() with toString(16) to convert each number to hexadecimal: arr.map(x => x.toString(16)).