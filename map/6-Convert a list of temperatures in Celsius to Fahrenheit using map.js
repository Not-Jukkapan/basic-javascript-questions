// Convert a list of temperatures in Celsius to Fahrenheit using map:

// Input: [0, 100, 37] → Output: [32, 212, 98.6]

// Hint: Use the formula (C * 9/5) + 32 inside .map() to convert each Celsius temperature: arr.map(c => (c * 9/5) + 32).
