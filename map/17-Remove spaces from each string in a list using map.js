// Remove spaces from each string in a list using map:

// Input: [" hello ", " world "] → Output: ["hello", "world"]

// Hint: Use .map() with replace() to remove spaces: arr.map(str => str.replace(/\s+/g, '')).