// Convert all strings in a list to uppercase using map:

// Input: ["a", "b", "c"] → Output: ["A", "B", "C"]

// Hint: Use .map() with toUpperCase() to convert each string to uppercase: arr.map(str => str.toUpperCase()).