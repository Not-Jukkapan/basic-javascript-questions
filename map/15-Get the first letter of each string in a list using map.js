// Get the first letter of each string in a list using map:

// Input: ["apple", "banana", "cherry"] → Output: ["a", "b", "c"]

// Hint: Use .map() with charAt(0) to return the first character: arr.map(str => str.charAt(0)).