// Find the exponential of each number in a list using map:

// Input: [1, 2, 3] → Output: [2.718, 7.389, 20.086]

// Hint: Use .map() with Math.exp() to find the exponential: arr.map(x => Math.exp(x)).
