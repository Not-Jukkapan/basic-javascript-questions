// Multiply each element by its index using map:

// Input: [5, 10, 15] → Output: [0, 10, 30]

// Hint: Use .map() with the second argument index to multiply each element by its index: arr.map((x, i) => x * i).