// Find the absolute values of numbers in a list using map:

// Input: [-1, -2, 3] → Output: [1, 2, 3]

// Hint: Use .map() with Math.abs() to get the absolute value of each number: arr.map(x => Math.abs(x)).