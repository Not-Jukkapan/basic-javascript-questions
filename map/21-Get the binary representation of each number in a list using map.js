// Get the binary representation of each number in a list using map:

// Input: [2, 3, 4] → Output: ['10', '11', '100']

// Hint: Use .map() with toString(2) to convert each number to binary: arr.map(num => num.toString(2)).