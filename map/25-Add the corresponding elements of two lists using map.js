// Add the corresponding elements of two lists using map:

// Input: [1, 2, 3], [4, 5, 6] → Output: [5, 7, 9]

// Hint: Use .map() to iterate through one list and add elements from the second list: arr1.map((x, i) => x + arr2[i]).