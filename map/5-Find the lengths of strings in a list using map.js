// Find the lengths of strings in a list using map:

// Input: ["apple", "banana", "cherry"] → Output: [5, 6, 6]

// Hint: Use .map() with the length property to return the length of each string: arr.map(str => str.length).