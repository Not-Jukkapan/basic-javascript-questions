// Convert a list of strings to floats using map:

// Input: ["1.1", "2.2", "3.3"] → Output: [1.1, 2.2, 3.3]

// Hint: Use .map() with parseFloat() to convert each string to a float: arr.map(str => parseFloat(str)).