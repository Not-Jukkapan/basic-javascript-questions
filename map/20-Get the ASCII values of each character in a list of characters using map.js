// Get the ASCII values of each character in a list of characters using map:

// Input: ["a", "b", "c"] → Output: [97, 98, 99]

// Hint: Use .map() with charCodeAt(0) to get the ASCII value: arr.map(ch => ch.charCodeAt(0)).