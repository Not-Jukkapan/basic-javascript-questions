// Convert each string in a list to lowercase using map:

// Input: ["HELLO", "WORLD"] → Output: ["hello", "world"]

// Hint: Use .map() with toLowerCase() to convert each string to lowercase: arr.map(str => str.toLowerCase()).