// Split a list into two halves:

// Input: [1, 2, 3, 4] → Output: ([1, 2], [3, 4])
// Hint: Use slice() to divide the list into two equal halves based on the midpoint: list.slice(0, mid) and list.slice(mid).
