// Find the cumulative sum of elements in a list:

// Input: [1, 2, 3] → Output: [1, 3, 6]
// Hint: Use the reduce() method and keep a running sum of all elements: list.reduce((acc, val) => acc + val).